image: docker:cli
services:
    - docker:dind
variables:
    DOCKER_DRIVER: overlay2
    COMPOSE_HTTP_TIMEOUT: 200 #incase of slow startup

stages:
    - build
    - test

build:
    stage: build
    script:
        - docker build -t .

test:
    stage: test
    script:
        - echo "tests not implemented yet..."
    